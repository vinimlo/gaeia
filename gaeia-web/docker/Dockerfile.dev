FROM node:20-alpine

WORKDIR /app

# Install dependencies first for better caching
COPY package*.json ./
RUN npm install

# Copy source files
COPY . .

# Expose dev server port
EXPOSE 4321

# Start dev server with hot reload
CMD ["npm", "run", "dev"]
